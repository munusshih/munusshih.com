<div class="bio">
  <details class="bio-description gsap-element" open>
    <summary class="serif text-default">About Munus</summary>
    <p class="monospace">
      Munus (t/he/y) is a Hakka Taiwanese artists, creative technologist, and
      educator in NYC. They blend code with design to create generative, bespoke
      and friendly interfaces for brands and communities.
    </p>
    <button class="email monospace" id="emailButton">munusshih@gmail.com</button
    >
    <p id="copyMessage">Email copied to clipboard!</p>
  </details>
  <div class="news monospace gsap-element">
    <p>What I've been up to:</p>
    <ul>
      <li>Working on revamping <a>p5.zine</a></li>
      <li>Co-designed the <a>p5 open call</a> for Processing Foundation</li>
      <li><a>DEMO Festival</a> at NEW INC</li>
    </ul>
  </div>
</div>
<script src="../scripts/copyEmail.js"></script>
<style>
  .bio {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 2rem;
    width: 100%;
    height: auto;
    color: rgb(var(--black));
  }

  .bio p {
    margin: 0;
  }

  details > summary {
    list-style: none;
  }
  details > summary::-webkit-details-marker {
    display: none;
  }

  .text-default {
    font-size: 1rem;
  }

  .bio button.email {
    cursor: pointer;
    border: none;
    background: none;
    padding: 0;
    color: var(--accent);
    font-size: 1rem;
    text-align: left;

    &:hover {
      text-decoration: underline;
    }
  }

  #copyMessage {
    display: none;
    color: rgb(var(--gray));
  }

  .bio-description {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 0;
    border: 1px dashed black;
    border-right: 0;
    border-left: 0;
    padding: 1rem;
    width: 100%;
    max-width: 50ch;
    color: rgb(var(--black));
    text-wrap: balance;
  }

  .news {
    border: 1px solid black;
    border-radius: 5px;
    background: rgb(var(--black));
    padding: 1rem;
    min-width: 25ch;
    max-width: 50ch;
    color: white;
  }

  .news ul {
    counter-reset: line-number;
    border-radius: 5px;
    background: #1e1e1e;
    padding-left: 0;
    padding: 1rem;
    color: #dcdcdc;
    font-family: monospace;
    list-style: none;
  }

  .news ul li {
    gap: 0.5em;
    counter-increment: line-number;
    margin-left: 1em;
    width: 100%;
    text-indent: -1em;
  }

  .news ul li::before {
    padding-right: 0.5em;
    content: counter(line-number);
    color: #858585;
    text-align: right;
  }

  .news *:nth-of-type(1) > a {
    color: #569cd6;
  } /* Blue */
  .news *:nth-of-type(2) > a {
    color: #d69d85;
  } /* Red */
  .news *:nth-of-type(3) > a {
    color: #b5cea8;
  } /* Green */
  .news *:nth-of-type(4) > a {
    color: #9cdcfe;
  } /* Light Blue */
  .news *:nth-of-type(5) > a {
    color: #c586c0;
  } /* Purple */
  .news *:nth-of-type(6) > a {
    color: #ce9178;
  } /* Orange */
  .news *:nth-of-type(7) > a {
    color: #dcdcaa;
  } /* Yellow */

  @media screen and (max-width: 768px) {
    .bio {
      flex-direction: column;
      gap: 0.5rem;
    }

    .bio-description {
      padding: 1rem 0;
    }

    .news {
      display: none;
    }
  }
</style>
