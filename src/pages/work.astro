---
import BasicLayout from "@/layouts/Basic.astro";
import Column from "@/components/Column.astro";
import LabCard from "@/components/LabCard.astro";
import labCards from "@/docs/lab.yml";
import { SITE_TITLE, SITE_DESCRIPTION } from "@/consts";

import { getCollection } from "astro:content";
const workItems = await getCollection("work");
---

<BasicLayout title={`Lab — ${SITE_TITLE}`} description={SITE_DESCRIPTION}>
  <main class="w-full px-4">
    <!-- <div
      class="grid grid-cols-1 items-start gap-x-4 gap-y-8 md:grid-cols-3 md:gap-y-20 lg:grid-cols-5"
    >
      {
        workItems.map((item) => (
          <div>
            <h2>{item.data.title}</h2>
            <p>{item.data.description}</p>
            <img src={item.data.heroImage} alt={item.data.title} />
            <p>
              <strong>Roles:</strong> {item.data.roles.join(", ")}
            </p>
            <p>
              <strong>Tags:</strong> {item.data.tags.join(", ")}
            </p>
            <p>
              <strong>Date:</strong> {item.data.pubDate.toDateString()}
            </p>
          </div>
        ))
      }
    </div> -->
  </main>
</BasicLayout>
